{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Nest: debug server-side",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run start:dev",
      "cwd": "${workspaceFolder}/server"
    }
  ]
}
